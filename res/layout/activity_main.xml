<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@color/oceanx_Color"
     >
	<Button
         android:id="@+id/publish_content_button"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:text="@string/publish_content_button"
         android:textSize="15sp"
          />
	<Button
         android:id="@+id/publish_link_button"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:text="@string/publish_link_button"
         android:textSize="15sp"
          />
	<Button
         android:id="@+id/publish_images_button"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:text="@string/publish_images_button"
         android:textSize="15sp"
          />
</LinearLayout>
